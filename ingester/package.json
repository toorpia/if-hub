{
  "name": "if-hub-pi-ingester",
  "version": "0.1.0",
  "description": "PI System data ingester for IF-HUB",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "js-yaml": "^4.1.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.5",
    "@types/node": "^20.8.0",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.2.2"
  }
}
