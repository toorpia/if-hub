# ============================================================================
# IF-HUB TimescaleDB Environment Configuration
# ============================================================================
# Copy this file to env.timescaledb and configure your settings
# Usage: cp env.timescaledb.example env.timescaledb
# ============================================================================

# Application Settings
NODE_ENV=development
PORT=3000
EXTERNAL_PORT=3001
TZ=Asia/Tokyo

# TimescaleDB Connection Settings
TIMESCALE_HOST=localhost
TIMESCALE_PORT=5432
TIMESCALE_DB=if_hub
TIMESCALE_USER=if_hub_user
TIMESCALE_PASSWORD=change_this_password

# Data Paths (default values - usually no need to change)
# STATIC_DATA_PATH=./static_equipment_data
# TAG_METADATA_PATH=./tag_metadata
# GTAG_PATH=./gtags
# LOG_PATH=./logs

# Logging
# LOG_LEVEL=info

# ============================================================================
# Docker Compose Usage
# ============================================================================
# Start services:
#   cd docker
#   docker-compose -f docker-compose.timescaledb.yml up -d
#
# View logs:
#   docker-compose -f docker-compose.timescaledb.yml logs -f
#
# Stop services:
#   docker-compose -f docker-compose.timescaledb.yml down
#
# Reset database (WARNING: deletes all data):
#   docker-compose -f docker-compose.timescaledb.yml down -v
# ============================================================================
